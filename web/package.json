{
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "axios": "^1.16.0",
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start:parcel": "npx parcel index.html",
    "start:db": "npx json-server -w db.json"
  }
}
